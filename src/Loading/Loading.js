import React from "react";
import loading from "../img/loading_money.gif";

const Loading = () => {
  return (
    <div>
      <img src={loading} />
    </div>
  );
};

export default Loading;
